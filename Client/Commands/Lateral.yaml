- Alias: connect
  Description: Connect to a TCP Drone
  Command: 90
  Arguments:
    - Key: target
      Optional: false
    - Key: port
      Optional: false

- Alias: link
  Description: Connect to an SMB Drone
  Command: 91
  Arguments:
    - Key: target
      Optional: false
    - Key: pipename
      Optional: false